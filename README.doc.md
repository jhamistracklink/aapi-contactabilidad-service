```php

  /**
   * @param Context-> devuelve la informacion del usuario en session
   * l
   * @link -> '/app/api/Utils/Service/Token.php'
  */
  $app->Context

  // para acceder a sus parametros
  $Ctx=$app->Context
  $Ctx->id;
  $Ctx->nombres;
  $Ctx->id_empresa;
  $Ctx->token;
   $Ctx->...;

```

```php

```